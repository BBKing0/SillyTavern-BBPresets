# BBPresets v0.4.0 开发预览

这是 SillyTavern 第三方浏览器扩展。当前完成本地自动测试和模拟宿主点检，第一次请用测试聊天验收。它独立于 BB-Memory，资料只保存在自己的文件和命名空间中。

## 安装

1. 仓库代码上传并可访问后，在酒馆 **扩展 → 安装扩展（Install extension）** 中输入 `https://github.com/BBKing0/SillyTavern-BBPresets`。按服务器权限选择当前账户或所有用户，等待安装完成。
2. 若手动安装，直接复制整个 `SillyTavern-BBPresets` 文件夹到云酒馆的 `SillyTavern/public/scripts/extensions/third-party/` 下。`manifest.json`、`index.js`、`style.css`、`core/`、`runtime/`、`ui/` 应直接位于插件目录内，不再多套一层文件夹。若使用账户专属扩展目录，按服务器原有扩展安装方式放置。无需 ZIP，也无需复制本地工作区外层的任何文件。
3. 刷新酒馆页面，在扩展设置中展开 **BBPresets v0.4.0**，直接选择概览、初始化、作者资料、收藏、维护、设置或恢复页。初始化、设置等完整功能就在酒馆折叠栏内，悬浮球只展开快捷菜单。不需要安装 npm 依赖。
4. 打开一个角色聊天，先在工作台“概览 / 存档”创建独立 BBPresets 存档，再运行初始化或手写条目。

插件要求酒馆提供 `SillyTavern.getContext()`、生成拦截器、`setExtensionPrompt`、聊天 metadata 保存、账户存储、localforage 和自有文件上传/读取能力。接口依据 2026-09 的官方 release 核查，但没有针对你的云酒馆版本完成实机测试。缺少能力或服务器写入失败会显示错误，不会静默变成仅本地保存。

## 从 v0.2.0 / v0.3.x 更新

用新目录中的文件覆盖同一 BBPresets 扩展目录中的运行文件，或在酒馆中更新原 Git 安装，再完整刷新页面（必要时强制刷新），卸载旧页面遗留的事件与模态窗口。不要另装第二份扩展；保留服务器资料和原存档，不删除 IndexedDB。

如果此前使用手动安装的 `BBPresets/`，本次也可以继续覆盖该目录。以后改用 Git URL 安装时，先备份资料并移除旧扩展的程序目录，再通过酒馆安装；不要同时保留 `BBPresets/` 与 `SillyTavern-BBPresets/` 两份运行入口。只处理扩展程序，不删除服务器存档。Git 安装后可在酒馆扩展管理中检查更新并刷新页面。

## 开始使用

完整工作台嵌在原生扩展折叠栏内，收起后保留当前页面和输入。在“设置”中可关闭、显示或重置悬浮球；球只提供常用快捷操作，不包含初始化和设置。拖动位置保存在本设备。

更新后完整刷新，验证原生标题展开/收起、七个功能页切换，以及关闭球后仍能从扩展栏操作。手机和横屏下，快捷菜单应始终避开聊天输入区。

1. **连接测试**：在“设置”选择主连接，或填写独立地址、模型和 Key，点“测试 API 连接”。成功后保存设置。测试发一条简短请求，不含故事资料。
2. **初始化**：点“读取资料，生成问题”，每题下直接回答，用上一题 / 下一题切换；可长答、留空，或在“自由补充 / 新想法”继续写。重新生成保留已答内容，补充问题参考已有回答。草稿自动保存在本设备并同步服务器，支持手动保存和导出。完成后点“建立初始资料”，半自动模式先审阅提案。生成问题、补问、建档分别需要模型请求。没有 200 字限制；若超过维护材料预算，会保留全文并提示提高预算，绝不静默截断。
3. **保护**：普通条目直接编辑一个正文框。可勾选“保护整个条目”，或选中正文文字后点“保护选中文字”；手机也可点“增加保护段”。只有需要局部保护时才分段，原有保护段保留。用户本人仍可编辑、解除保护。
4. **正常 RP**：0 楼开场白不算完整轮。发送第 3 楼时整理 1/2 楼，发送第 5 楼时整理 3/4 楼；只输入但未发送不会提取。重 roll、重新生成和续写不触发自动提取。本次正文冻结使用已经提交的资料，晚到维护影响后续回复，不会重写聊天正文。
5. **点评**：在聊天回复中划选文字后打开工作台，或点“载入最新回复”并删减原文。可以只收藏、加入批次、立即发送。批次只有点击发送才交给模型。
6. **指南**：故事指南优先于用户默认指南；可将故事中一条指南复制为用户默认副本。作者自我总结与明确用户偏好分开。临时要求只用于下一次回复。

没有点评不代表认可。指南是作者写作参考，不能把“喜欢冲突”直接改写成世界已经发生灾难。维护一次也不等于世界前进一天。

## 模式、连接与成本

| 设置 | 行为 |
| --- | --- |
| 半自动（默认） | 小幅世界变化自动应用；重要变更、初始化、反馈升级先作为提案 |
| 全自动 | 自动修改全部未保护内容，包括重要变化；保护仍有效 |
| 全手动 | 不自动新建维护任务，手动按钮产生的变更先审阅 |
| 发送时后台 | 正文不等待维护模型；来源固定为发送瞬间已完成的轮次 |
| 先维护再生成 | 先等待本次维护，再构建正文资料；会增加等待时间 |
| 主连接（默认） | 沿用酒馆连接，后台任务在正文完成后排队调用 |
| 独立连接 | 配置 OpenAI 兼容 API 地址、模型与 Key，可在正文期间维护 |

世界维护频率为 N 时，会将累计 N 轮合为一次请求；作者总结另有频率。界面显示当前聊天的最新提取楼层、最近自我总结时间和队列状态；提案尚未应用时会标注“待审阅”。记录随存档保存，刷新不归零。主连接和独立连接都会增加维护请求；没有实现“正文同一次响应内更新资料”。实际 token 取决于模型、资料和输出，字符预算不是 token 数。

独立连接直接由浏览器访问 API，需要该服务允许跨域；HTTPS 酒馆无法访问被浏览器拦截的 HTTP API。Key 仅存在本设备 localforage，不进入服务器文档或导出；手机使用独立连接时需自行填写 Key。主连接凭据继续由酒馆管理。

主连接的底层维护请求不一定能够独立取消。超时/切档会停止应用结果，插件不调用酒馆全局停止按钮。维护尚未结束又立即发送下一条时，底层请求可能仍共享宿主状态，需要真实酒馆兼容验收；独立连接更适合连续后台使用。

## 后台与网络恢复

切浏览器标签、折叠侧栏或暂时进入后台不会主动中止正在初始化或维护的请求。后台不启动新的自动任务，已开始的请求仍可完成并保存；返回页面会在当前任务结束后刷新服务器版本。切聊天/切档、改连接或主动停止仍会取消旧任务，旧结果不会写入新故事。

网络/超时/408/429/部分 5xx 最多自动重试两次，显示重试进度；断网时等待 online 事件，输入与任务保留。401 等配置错误和 JSON/保护校验错误不自动重试。维护结果已校验但保存失败时会缓存，手动重试或网络恢复后先检查版本与来源，再复用结果，减少重复模型调用。

浏览器被系统冻结或关闭时，纯浏览器插件不能保证请求不断。重新打开后恢复已保存问答与维护任务；未完成的提问可重新生成 / 补充继续，已答不会丢失。主连接底层仍有请求时不会强行开启第二个。

## 存档、分支与 BB-Memory

- “保存当前存档”或球菜单“快速保存”会先保存初始化草稿，再创建服务器版本。只在回读确认后显示成功；在“恢复 / 导出”可找到历史版本。条目编辑框中的修改仍需先点“保存条目”。
- 换楼续写可以选同一档；从较早楼层开 if 时，在新聊天选择“从原聊天复制 if 分支”，复制有效历史，子档独立发展。原聊天已失效来源不应进入子档。
- 默认保留两个插件各自的选择。首次要在两个插件分别选好档，然后点击“确认当前两个存档属于同一故事”。读取与跟随是两个独立开关。
- 未安装 BB-Memory 时可独立使用。启用跟随但当前槽无法验证时暂停 BBPresets 注入/维护；关闭跟随可继续独立使用。
- 当前桥接只支持已考察的 BB-Memory `chatSlotBindings` 和带 `_slotCreatedAt` 的槽数据，不猜测旧版本身份。不会修改 BB-Memory 文件、角色卡或世界书。

## 手机与电脑接续

电脑和手机访问同一个云酒馆、同一账户，一次只在一台设备使用。等待状态显示 **已保存到酒馆服务器** 后再换设备；另一台打开页面会读取服务器记录，旧页面恢复前台时也会刷新。

服务器保存流程为新版本写入、回读、索引备份、索引发布与回读。普通浏览器文件接口没有服务器 CAS，不承诺两台设备同时写入的事务安全；也不需要设备接管或主设备选择。

保存失败时，到“恢复 / 导出”检查本设备未完成保存，可导出或恢复为独立副本。不要把本机待恢复副本当作已同步到另一台设备。网络恢复后可重新读取服务器并手动合并副本。

## 撤回与恢复

- 编辑、重抽或删除来源消息后，世界派生会回滚或暂停注入。受保护或已被你手改的文字不覆盖，另列冲突。检查条目后重新保存可明确确认你的版本。
- 收藏/点评原文保留，标记来源已变化。已发送点评不会因删楼自动消失；可以单独撤回，相应派生指南会暂停注入。
- 历史版本可先查看再恢复，恢复生成新版本并保留审计历史与当前点评。当前保护和旧版本冲突时停止恢复；恢复资料版本不会清空当前初始化草稿。
- 导出包含当前资料及文档变更日志，不含 API Key，也不逐个打包服务器上全部历史快照文件。导入创建新档，不覆盖已有资料；用户默认指南导入为一个可检查的故事副本。
- AI 维护不能清空恢复历史。历史版本会占用服务器空间；单档超过 400 万字符时需导出并建立新档。索引和备份同时丢失后，普通文件接口无法枚举孤立版本自动找回，应保留独立导出。

## 已知预览限制

初始化读取角色卡/内嵌书、用户人设、当前绑定的角色/聊天/人设世界书、全局及角色附加世界书和近期对话；BB-Memory 只在已启用且确认同故事时读取。全局/附加书发现依赖宿主模块能力，缺少能力或部分书读取失败会显示说明。只读当前绑定书，不遍历无关故事；每次最多 32 本，按预算节选。超出预算的条目会省略并显示数量，不能保证每条资料每次都进入模型。

剧情质量、长线铺垫效果、真实模型 JSON 成功率、BB-Memory 版本兼容和云服务器跨设备保存都需要实机测试。预览没有访问你的云酒馆，也没有使用真实 API 验证这些效果。

## 快速验证

1. 更新并完整刷新；展开 BBPresets 原生折叠栏，在测试聊天建档。逐题输入超过 200 字的回答，下一题/上一题、收起/展开、切设置再返回和刷新页面，问题与回答应保留。测试重新生成、补充问题，自由补充和历史回答都应保留。
2. 正在生成问题时切到其他浏览器标签，稍后返回，应继续或完成而非因隐藏页面取消。切 Wi-Fi/移动网络时应显示等待/有限重试；失败时仍能看见输入。只在测试聊天验证，系统强制冻结/关闭页面需重新打开后恢复。
3. 新建条目应只有一个正文框；选一段文字保护并保存，原文逐字一致。全自动维护不得改动该段。整条保护与局部保护分别检查。
4. 从球菜单快速保存，确认“已保存到酒馆服务器”，刷新或用另一设备同云账户打开同一聊天，应看到相同存档、问答和进度。关闭悬浮球，初始化/设置仍可从折叠扩展栏完成。

重点边界：请求未完时切到另一故事，结果不得串档；重抽/编辑已提取消息后，该来源的提取楼层不得继续冒充有效，而保护文字与收藏原文仍保留。
