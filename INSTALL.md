# BBPresets v0.5.2 开发预览

BBPresets 是个性化作者助手：读者划线评形成写作建议，多条故事线维持长期节奏，建议与大纲进入正常 RP 上下文。预设仍负责扮演。以下流程已在模拟宿主验证，第一次请用测试聊天验收。

## 安装与升级

1. 仓库上传后，在酒馆 **扩展 → 安装扩展** 输入 `https://github.com/BBKing0/SillyTavern-BBPresets`。也可直接复制本目录到酒馆 `public/scripts/extensions/third-party/SillyTavern-BBPresets/`；无需 npm、编译或 ZIP。
2. 升级时更新同一份扩展并完整刷新。旧手动目录名为 `BBPresets/` 也可继续覆盖；不要同时保留两份运行入口，不删除服务器资料或 IndexedDB。
3. 打开测试聊天，展开 **BBPresets v0.5.2**，在“概览”创建 BBPresets 存档。
4. v0.5.0 的大纲、保护、草稿与历史版本保留，原用户指南作为默认作者。旧故事中的写作偏好与点评保留，进入“个性化作者 → 旧故事中的写作偏好与点评（保留）”，点击“复制旧故事偏好为作者”即可复制并选用；原故事不变。旧故事偏好不再直接注入，避免混入其他作者。更早的提取 / 自总结待办继续归档停用。

插件需要酒馆的 `getContext()`、生成拦截器、`setExtensionPrompt`、metadata、账户存储、localforage 和自有文件上传 / 读取接口。控制块显示过滤还需要内置 Regex 扩展。已核查官方 release 源码；你的具体酒馆与其他扩展组合仍需实测。

## 页面入口

| 分类 | 内容 |
| --- | --- |
| 概览 | 无剧透状态、保存 / 新建 / 绑定 / 导入导出大纲、服务器加载 |
| 故事 | 初始化、大纲条目；主动调整故事线也在大纲页 |
| 作者 | 个性化作者、划线评 |
| 任务 | 进行中与待处理、待审阅结果、冲突和折叠的变更历史 |
| 工具 | 设置、提示词、注入预览、版本恢复 |

主分类只显示所属页面；切回分类会回到该分类上次打开的页面。顶部显示当前大纲、作者和请求 / 保存状态，计数与章节详情在“运行详情”展开。悬浮球保留常用操作与收藏 / 点评。

## 主副连接与初始化

在“工具 → 设置”选择 **点评总结连接 → 独立副 API**，填写 OpenAI 兼容地址、模型和 Key，测试后保存。该连接仅用于点评总结，也可选择复用主连接。**初始化提问、补问、建立与修订大纲固定使用酒馆当前主连接**，请确保主模型正常可用；不会因副 API 失败而暗中切换模型。

副 API 从浏览器直接访问，需要服务端允许跨域。HTTPS 酒馆通常要求 HTTPS API。Key 仅保存在本设备，不随服务器文档或提示词导出同步；手机需自行填写。主连接凭据仍由酒馆管理。

先在“工具 → 设置”的“主 API · 初始化与大纲”点击“测试主 API”，确认酒馆连接可用。该测试独立于点评连接；不会发送故事资料。

在“故事 → 初始化”点“读取资料，生成问题”，主模型读取可用记忆、近期上下文、世界书和角色资料，生成 2—3 个简短问题。逐题作答，可长答、留空、自由补充、补问和重新提问。示例不作为你的回答。问题和回答按故事 / 聊天自动保存，切页与折叠保留输入。

点“建立初始资料”后，主模型结合回答、可读世界书、角色 / 用户资料、近期上下文、已确认的记忆与已有作者资料，建立：

- **故事核心**：主题、世界约束与期望体验，不预设总终局。
- **故事线**：按需要分为人物、神明、战争、地区发展等线；记录阶段目标、冲突、推进条件和可变路径。
- **章节与伏笔**：当前阶段方向、关联线索、前置与回收条件。

当前作者偏好只作为参考。初始化只生成大纲，不覆盖作者写作偏好；长期写作偏好在“个性化作者”维护，或通过划线评总结。

全自动模式直接应用未保护内容；半自动默认生成初始化提案，在“任务队列”查看并应用。状态会区分排队、请求、解析、保存与失败。

遇到 JSON 错误时，在“任务队列”查看失败任务和“本设备模型响应”，然后点“重试此任务”。完整围栏、解释包装可解析；截断、多个结果和非法修改不会强行应用。原回答保留，已校验结果若只是在保存时失败，可在来源与版本仍一致时复用。原始诊断仅在本设备，可能含剧透，不会自动公开。问题生成失败则在初始化页重新生成 / 重试。

如果主 API 响应较慢，达到设置里的“主 API 慢响应提醒 / 副 API 超时”时长后，界面显示继续等待原请求，**不会另发重复请求或丢弃稍后到达的有效结果**。副 API 超时仍会取消当次请求，并按网络错误规则有限重试。

“停止当前请求”停止插件等待并拒绝晚到结果，问答和任务保留。酒馆底层主请求可能仍在运行，此时会说明是上一条 BBPresets 请求占用，还是正在生成正文；等待其结束后再重试。插件不强行清空忙碌状态、不停止用户的正文。

回答不设 200 字限制；超出材料预算会保留输入并提示提高预算。世界书 / 记忆会按能力与预算节选，界面显示读取说明，不代表全量阅读。

## 正常 RP 与按需改纲

每轮发送前，插件按预算注入故事核心、相关故事线、写作建议和精简目录。选条结合上轮推荐与当前用户输入关键词；在“注入预览”可查看实际发送快照、省略数和本次会话请求统计。

主模型在正文末尾附一个 `[BBP_CONTROL]…[/BBP_CONTROL]` 块，包含：

| 内容 | 行为 |
| --- | --- |
| 当前章节名、简短进度 | 本地保存，不额外请求 API |
| 下轮大纲条目 ID | 本地保存，参与下轮注入 |
| 修改大纲的条目、理由和意图 | 排入一次主模型修订任务 |

主模型修订会读取目标大纲、关联资料、近期正文及可用记忆。修订结果不会再递归触发新请求。普通 RP 不再按楼层提取，也不再定时作者自总结。章节可以持续多轮，故事线可自然收束、休眠或发展后续。

下一条正文默认等待未完成修订。等待面板和悬浮菜单提供 **沿用旧大纲继续**，本次使用等待前的快照；后台新结果只影响后续。若主连接请求已经开始，仍须等它释放后再发正文；等待超时会提示稍后重发。修订失败或提案未应用时保持等待，可重试、应用 / 拒绝提案或选择旧纲。

在“故事 → 大纲条目 → 主动调整故事线”填写要求，可主动请求主模型。缺少 / 非法 / 被停止的控制信息不触发额外兜底请求，不应用大纲更新；状态显示原因，可主动修订。刷新页面不会恢复尚未完成正文的本轮令牌。

控制过滤只改显示和发送给模型的历史副本，原始消息仍含控制块。停用 Regex 时可能看见它；其他直接读取原始聊天的扩展也可能读到它。插件停用 / 卸载时移除自己的规则，不删其他正则，不改原文。

## 划线评与写作建议

侧边栏“划线评”和悬浮球“收藏 / 点评”都提供独立的原文框、点评框和态度选择。正文划选后点击“载入选中段落”，也可“载入最新回复”后删减，或点“手动粘贴原文”进入原文框粘贴。评论写在“点评”框中，再选择：

- **只保存**：不交给模型，不计入自动阈值。
- **加入批次**：默认累计 5 条已提交而未处理的点评后总结；可在设置修改数量，或提前点击批量发送。
- **立即发送**：提交当前点评。

全手动模式不自动按阈值运行。失败不会消费点评，待审阅 / 正在处理的批次不会重复提交。点评与总结出的建议属于当前选定作者，跨聊天沿用；切换大纲不改变作者。任务队列显示当前大纲和当前作者的任务，未选作者的待办保留，切回后可继续。没有点评不表示认可，作者猜测不能冒充用户偏好。撤回点评后，相应派生建议暂停注入。

## 大纲与个性化作者

“概览”展示无剧透统计，并提供保存存档、导出当前大纲、导入、新建故事、绑定大纲与从服务器加载。大纲绑定当前聊天；新聊天需单独选择或建立大纲。

“个性化作者”提供作者切换、重命名、新建、复制、导出和导入，也可直接编辑写作建议。新建 / 复制后立即选用新作者；导入创建独立副本，需要从列表选择。作者编辑会自动保存到服务器。当前作者选择跟随酒馆账户，不自动按酒馆的用户人设切换。没有大纲时也可使用作者偏好和点评。

大纲导入仍是大纲，作者导入仍是作者；两者都不会覆盖现有资料。旧用户指南文件作为作者导入。设置与提示词仍为账户共用，不因切换作者而改变。已移除“本轮要求”功能。

## 编辑与提示词

“大纲条目”管理故事资料，默认遮蔽剧透；“个性化作者”只展示作者偏好。两页都支持条目逐条或全部折叠。搜索标题、关键词、摘要和完整正文，折叠条目也能命中并展开；清空搜索恢复展开状态。返回列表保留条目编辑草稿，重新打开可继续；保存到服务器仍需点“保存条目”。未保存的条目编辑不承诺刷新页面后恢复。

普通条目用一个正文框；可整条保护或选文字保护，手机也可增加保护段。全自动只修改未保护内容，不改变保护规则。用户本人可编辑或解除保护。

“工具 → 提示词”按通用规则、初始化与大纲、划线评与正文、连接与历史兼容分组，提供全部模板的编辑、恢复默认、保存、整套导出与导入。`{{material}}`、`{{contract}}`、`{{token}}` 等必需占位符须保留；保护和 JSON 校验规则不会因编辑提示词而失效。修改协议说明时仍需遵守当前字段结构。模板随账户保存，导出只包含已保存提示词，不含故事或 Key；有未保存内容会提示，切页保留草稿。升级保留自定义模板；如果旧初始化模板要求生成 guide，请更新为只生成 core/line/chapter/clue，或恢复“主 API · 建立多线大纲”的默认模板并保存。

| 编辑权限 | 结果处理 |
| --- | --- |
| 半自动（默认） | 小改动自动应用；重要变更、初始化、点评总结先审阅 |
| 全自动 | 自动应用全部未保护内容，包括重要变更 |
| 全手动 | 只按按钮运行；正文改纲信号保留为待手动运行，变更先审阅 |

## 保存、分支与设备接续

“概览 → 保存存档” / 球菜单“快速保存”先保存初始化草稿，再创建可恢复服务器版本；条目正文需先点保存条目。保存只在回读确认后显示成功。遇到服务器失败，可在“版本恢复”查看本设备未完成保存并导出 / 恢复为独立副本。

编辑、重抽或删除来源后，相关大纲回滚或暂停注入，章节控制失效；保护文字及后来手动修改不被覆盖，出现冲突提示。从早期聊天开 if 时选择复制原档，按分叉前有效来源继承，子档独立发展。

BB-Memory 独立选择存档，跟随只切换故事大纲，不改变当前作者。只有确认同故事且开启读取才只读访问；跟随是另一个可选开关。缺少插件仍可独立使用，无法确认映射时不猜测或改写别的故事。当前桥接依赖已考察的存储格式，并非稳定公共协议。

电脑 / 手机使用同一个云酒馆账户，一次一台设备；换设备前等“已保存到酒馆服务器”。打开页面或恢复前台会读取服务器版本。服务器文件接口没有 CAS，不保证两台同时写入安全，不需要主设备 / 接管设置。

后台隐藏不会主动取消已开始的请求；前台恢复后刷新。短暂网络故障最多自动重试两次，离线等恢复；JSON / 配置 / 保护错误需手动处理。切档或停止后的晚到结果不应用。系统冻结 / 关闭页面后不能保证网络继续，重新打开可恢复已保存输入和任务。

历史恢复新增版本，不清空日志和点评，不覆盖当前保护文字。导入创建副本；导出包括当前资料和变更日志，不包含 Key 或全部服务器历史快照。单档上限 400 万字符、控制记录 5000 条；接近上限时应导出归档并建立新档，历史快照仍占服务器空间。

## 快速验收

1. 在“工具 → 设置”测试主 API，然后在“故事 → 初始化”提问、补问和建纲。将点评连接设为副 API，这些操作仍应全部走主模型；读取已确认记忆、上下文与可用世界书，回答后出现故事核心和故事线。失败时回答保留，半自动需应用提案。
2. 慢模型超过提醒时长时应显示继续等待原请求，完成后仍出现问题 / 大纲，不能反复新建请求。按停止则保留输入，原连接释放后可重试。
3. 在“作者 → 个性化作者”新建作者并增加建议，切换聊天 / 大纲后作者保持不变；手动切换作者后，大纲绑定保持不变，注入预览改用选定作者。
4. 将条目收起后搜索正文词，应找到并展开；编辑后返回列表再打开，草稿保留。保护一段后请求改纲，原文不得变化。
5. 连续 RP：普通章节 / 选条更新不增加策划调用；发生改纲才增加主请求。修订中发送下一条，观察等待和“沿用旧大纲继续”。
6. 从球菜单载入选段或粘贴，原文与点评应分别保存。只保存点评应无调用；提交到阈值后副 API 总结一次，应用后在注入预览看到写作建议。提示词修改保存后导出、导入，内容一致。
7. 等服务器保存成功，再用手机同账户打开同一聊天，检查故事、当前作者、提示词和问答；测试窄屏操作及球位置。

重点边界：主 API 慢响应期间切聊天或停止，晚到结果不能写入新档；正在生成正文时初始化应暂缓，不能抢占连接。点评未返回时切作者也不得串档。改纲未完切聊天、停止或重抽，不得串档；保护文字与早期 if 不得被后续计划覆盖。真实模型的协议遵守、流式显示、BB-Memory 同用及云端保存，需要在你的酒馆环境完成上述验收。
